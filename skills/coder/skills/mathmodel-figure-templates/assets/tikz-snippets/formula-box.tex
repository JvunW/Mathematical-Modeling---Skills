% A formula is useful only when it is part of the figure's causal explanation.
\begin{scope}[font=\small]
  \node[
    draw=blue!55!black,
    fill=blue!7,
    rounded corners=2pt,
    line width=0.65pt,
    inner xsep=9pt,
    inner ysep=7pt,
    text width=5.0cm,
    align=center
  ] (formulaHero) at (0,0) {$x^{\star}=\arg\min_{x\in\Omega} f(x)$\\[-1pt]
    \scriptsize feasibility first, objective second};
\end{scope}
